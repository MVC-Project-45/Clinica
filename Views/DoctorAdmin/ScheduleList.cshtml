<<<<<<< HEAD
﻿@model MVC_Final.ViewModels.DoctorAdminViewModel

<h2>Doctor's Schedule</h2>

@if (Model.Schedules != null && Model.Schedules.Any())
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Date</th>
                <th>Day of Week</th>
                <th>Start Time</th>
                <th>End Time</th>
                <th>Duration</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var schedule in Model.Schedules)
            {
                <tr>
                    <td>@schedule.Date.ToShortDateString()</td>
                    <td>@schedule.DayOfWeek</td>
                    <td>@schedule.StartTime.ToString("HH:mm")</td>
                    <td>@schedule.EndTime.ToString("HH:mm")</td>
                    <td>@schedule.Duration minutes</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@schedule.Id" class="btn btn-sm btn-primary">Edit</a>
                        <a asp-action="Delete" asp-route-id="@schedule.Id" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No schedules found for this doctor.</p>
}

<div class="mt-3">
    <a asp-action="Schedule" class="btn btn-success">Add New Schedule</a>
</div>

@section Scripts {
    <script>
        $(function () {
            $('table').DataTable();
        });
    </script>
=======
﻿@model MVC_Final.ViewModels.DoctorAdminViewModel

<h2>Doctor's Schedule</h2>

@if (Model.Schedules != null && Model.Schedules.Any())
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Date</th>
                <th>Day of Week</th>
                <th>Start Time</th>
                <th>End Time</th>
                <th>Duration</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var schedule in Model.Schedules)
            {
                <tr>
                    <td>@schedule.Date.ToShortDateString()</td>
                    <td>@schedule.DayOfWeek</td>
                    <td>@schedule.StartTime.ToString("HH:mm")</td>
                    <td>@schedule.EndTime.ToString("HH:mm")</td>
                    <td>@schedule.Duration minutes</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@schedule.Id" class="btn btn-sm btn-primary">Edit</a>
                        <a asp-action="Delete" asp-route-id="@schedule.Id" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No schedules found for this doctor.</p>
}

<div class="mt-3">
    <a asp-action="Schedule" class="btn btn-success">Add New Schedule</a>
</div>

@section Scripts {
    <script>
        $(function () {
            $('table').DataTable();
        });
    </script>
>>>>>>> b905b14e014d082ee4d3e4a1db7d5c994c74a9c2
}