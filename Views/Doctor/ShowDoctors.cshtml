<<<<<<< HEAD
﻿@model List<Doctor>;
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<html lang="ar" dir="rtl">
<style>
    .doctor-card {
        max-width: 900px;
        margin: auto;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        padding: 1rem;
    }

    #d1 {
        display: flex;
        justify-content: space-between;
    }

    #head:hover {
        cursor: pointer;
    }

    .doctor-card .card-body img {
        width: 90px;
        height: 100px;
        display: inline-flex;
        float: right;
        margin-top: -2rem;
        margin-left: 1rem;
    }

    .doctor-card .card-body {
        padding: 20px;
    }

    .doctor-card .doctor-info {
        margin-bottom: 10px;
    }

        .doctor-card .doctor-info strong {
            display: inline-block;
        }

    .line {
        display: inline;
    }

    #head {
        text-decoration: none;
        color: black;
    }
</style>

<div class="Container" style="padding:5rem">
    <div class="row">
        <div class="col-md-2">
        </div>
        <div class="col-md-6">
            <form class="d-flex" asp-action="Index" asp-controller="doctor" method="get">
                <input name="searchString" class="form-control me-1" type="search" placeholder="بحث..." aria-label="Search">
                <button class="fa fa-search" type="submit"></button>
            </form>

        </div>
        <div class="col-md-3">
            <a asp-action="Index" method="get" class="text-dark">عرض الجميع</a>
        </div>
    </div>


    <div class="row">
        <div class="col-md-12 p-4">
            <table class="table">
                @foreach (var doctor in Model)
                    if (doctor.Specializations != null)
                    {
                        <tr>
                            <div class="doctor-card card mb-3">
                                <div class="card-body">
                                    <div id="d1">
                                        <a id="head" class="doctor-info" asp-action="Profile" asp-route-id="@doctor.Id" method="post">
                                            <img src="/image/@doctor.Img" class="card-img-top">
                                            <h5 class="card-title  line">دكتور @doctor.Name</h5>
                                        </a>
                                        <div class="doctor-info  line">
                                            <strong>التخصص: </strong>

                                            @doctor.Specializations.Name


                                        </div>
                                        @if (doctor.Reviews.Count == 0)
                                        {
                                            <partial name="_Rate" model="@doctor.TotalRate" />

                                        }
                                        else
                                        {
                                            <partial name="_Rate" model="@doctor.TotalRate/doctor.Reviews.Count" />
                                        }
                                    </div>
                                    <div class="doctor-info">
                                        <strong>سعر الكشف : </strong>@doctor.Price جنيها
                                    </div>
                                    <div class="doctor-info">
                                        <strong>العنوان : </strong>@doctor.Address
                                    </div>
                                    <a href="#" class="btn btn-primary">حجز موعد</a>
                                </div>
                            </div>
                        </tr>
                    }
            </table>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    function openProfile(ID) {
        window.location.href = "/doctor/profile/" + ID;
    }
</script>
=======
﻿@model List<Doctor>;
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<html lang="ar" dir="rtl">
<style>
    .doctor-card {
        max-width: 900px;
        margin: auto;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        overflow: hidden;
        padding: 1rem;
    }

    #d1 {
        display: flex;
        justify-content: space-between;
    }

    #head:hover {
        cursor: pointer;
    }

    .doctor-card .card-body img {
        width: 90px;
        height: 100px;
        display: inline-flex;
        float: right;
        margin-top: -2rem;
        margin-left: 1rem;
    }

    .doctor-card .card-body {
        padding: 20px;
    }

    .doctor-card .doctor-info {
        margin-bottom: 10px;
    }

        .doctor-card .doctor-info strong {
            display: inline-block;
        }

    .line {
        display: inline;
    }

    #head {
        text-decoration: none;
        color: black;
    }
</style>

<div class="Container" style="padding:5rem">
    <div class="row">
        <div class="col-md-2">
        </div>
        <div class="col-md-6">
            <form class="d-flex" asp-action="Index" asp-controller="doctor" method="get">
                <input name="searchString" class="form-control me-1" type="search" placeholder="بحث..." aria-label="Search">
                <button class="fa fa-search" type="submit"></button>
            </form>

        </div>
        <div class="col-md-3">
            <a asp-action="Index" method="get" class="text-dark">عرض الجميع</a>
        </div>
    </div>


    <div class="row">
        <div class="col-md-12 p-4">
            <table class="table">
                @foreach (var doctor in Model)
                    if (doctor.Specializations != null)
                    {
                        <tr>
                            <div class="doctor-card card mb-3">
                                <div class="card-body">
                                    <div id="d1">
                                        <a id="head" class="doctor-info" asp-action="Profile" asp-route-id="@doctor.Id" method="post">
                                            <img src="/image/@doctor.Img" class="card-img-top">
                                            <h5 class="card-title  line">دكتور @doctor.Name</h5>
                                        </a>
                                        <div class="doctor-info  line">
                                            <strong>التخصص: </strong>

                                            @doctor.Specializations.Name


                                        </div>
                                        @if (doctor.Reviews.Count == 0)
                                        {
                                            <partial name="_Rate" model="@doctor.TotalRate" />

                                        }
                                        else
                                        {
                                            <partial name="_Rate" model="@doctor.TotalRate/doctor.Reviews.Count" />
                                        }
                                    </div>
                                    <div class="doctor-info">
                                        <strong>سعر الكشف : </strong>@doctor.Price جنيها
                                    </div>
                                    <div class="doctor-info">
                                        <strong>العنوان : </strong>@doctor.Address
                                    </div>
                                    <a href="#" class="btn btn-primary">حجز موعد</a>
                                </div>
                            </div>
                        </tr>
                    }
            </table>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    function openProfile(ID) {
        window.location.href = "/doctor/profile/" + ID;
    }
</script>
>>>>>>> b905b14e014d082ee4d3e4a1db7d5c994c74a9c2
